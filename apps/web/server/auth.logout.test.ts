import { describe, it, expect } from "vitest";

describe("auth logout (legacy)", () => {
  it("placeholder", () => {
    // Logout agora limpa cookie "nexo_token" via session router.
    expect(true).toBe(true);
  });
});
