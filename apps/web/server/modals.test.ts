import { describe, it, expect } from "vitest";

describe("modals (legacy)", () => {
  it("placeholder", () => {
    // O portal migrou do drizzle/mysql para proxy do Nest.
    // Este teste antigo existia para validar modais + db local.
    expect(true).toBe(true);
  });
});
